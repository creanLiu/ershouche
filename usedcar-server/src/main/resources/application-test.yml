spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usedcar_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 1

wx:
  miniapp:
    app-id: test-app-id
    app-secret: test-app-secret

jwt:
  secret: usedcar-test-jwt-secret-key-2024-must-be-at-least-256-bits-long
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

logging:
  level:
    com.usedcar: debug
